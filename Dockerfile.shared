FROM oven/bun:alpine

WORKDIR /app

# Instalar dependências necessárias para o Prisma
RUN apk add --no-cache openssl

# Copiar todo o diretório shared
COPY apps/shared /app

# Instalar dependências
RUN bun install

# Executar o script de migração diretamente
CMD ["bun", "run", "migrate.ts"]

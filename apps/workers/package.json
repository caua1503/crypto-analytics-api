{
	"dependencies": {
		"@prisma/adapter-pg": "^7.2.0",
		"@prisma/client": "^7.2.0",
		"@repo/shared": "../shared",
		"bullmq": "^5.66.5",
		"dotenv": "^17.2.3",
		"pg": "^8.17.1",
		"prisma": "^7.2.0",
		"zod": "4.4.1"
	},
	"name": "workers",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@types/bun": "latest"
	},
	"scripts": {
		"build": "bun i && bun run lint && bun build --compile --minify --sourcemap --bytecode src/index.ts --outfile ../../build/workers",
		"dev": "bun i && bun run lint && bun --watch run src/index.ts",
		"lint": "bunx @biomejs/biome check --write"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}

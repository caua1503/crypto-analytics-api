{
  "dependencies": {
    "@prisma/adapter-pg": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "@repo/shared": "../shared",
    "bullmq": "^5.66.5",
    "dotenv": "^17.2.3",
    "pg": "^8.17.1",
    "prisma": "^7.2.0",
    "zod": "^4.3.5"
  },
  "name": "workers",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "bun build --compile --minify --bytecode src/index.ts --outfile workers",
    "dev": "bun run lint && bun --watch run src/index.ts",
    "lint": "bunx @biomejs/biome format --write"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}